<Window x:Class="MolochnyKombinat.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация - Молочный комбинат Полесье" 
        Height="359" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinWidth="400" MinHeight="350">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        
        <TextBlock Grid.Row="0" Text="Вход в систему" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

       
        <TextBlock Grid.Row="1" Text="Логин:" FontSize="14" Margin="0,5"/>
        <TextBox Grid.Row="2" Name="txtLogin" Margin="0,0,0,15" 
                 FontSize="14" Padding="5" Height="35"/>

      
        <TextBlock Grid.Row="3" Text="Пароль:" FontSize="14" Margin="0,5"/>
        <PasswordBox Grid.Row="4" Name="txtPassword" Margin="0,0,0,20" 
                     FontSize="14" Padding="5" Height="35"/>

       
        <Button Grid.Row="5" Name="btnLogin" Content="Войти" FontSize="16" 
                Height="40" Width="150" HorizontalAlignment="Center"
                Background="#4CAF50" Foreground="White"
                Click="btnLogin_Click"/>

        <!--сообщения об ошибках-->
        <TextBlock Grid.Row="6" Name="txtStatus" Text="" Foreground="Red" 
                   TextAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</Window>